bind 127.0.0.1 ::1
protected-mode yes
port 6379
tcp-keepalive 300
timeout 0
supervised systemd
appendonly yes
appendfsync everysec
maxmemory-policy noeviction

# Set a strong ACL/password during installation and copy it to REDIS_PASSWORD.
# Never commit the production credential to this file.
